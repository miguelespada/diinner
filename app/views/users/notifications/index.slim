.row.box
  .diinner-label.yellow
    = t("notifications")
  .notification-list.full
    - if @notifications.any?
      = render_activities(@notifications.to_a)
      = will_paginate @notifications
    - else
      .notification
        .message
          | No tienes notificaciones.