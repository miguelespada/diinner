/ TODO use device and understand when to use a partial for form_fields
h1 Edit Password
= simple_form_for([@restaurant], :url => :restaurant_password, :method => :put) do |f|
  = render "password_form_fields", :f => f