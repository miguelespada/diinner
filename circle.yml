machine:
  ruby:
    version: 2.0.0
  services:
    - elasticsearch
test:
 override:
   - bundle exec rspec
   - bundle exec cucumber
deployment:
  staging:
    branch: master
    heroku:
      appname: diinner